import React from 'react';
import { useHistory } from 'react-router-dom';

const HeaderSearch = ({ searchInput }) => {
  const history = useHistory();

  return (
    <div id="header-search-list">
      <p>asd</p>
    </div>
  );
};

export default HeaderSearch;
