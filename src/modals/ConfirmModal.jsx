import React from 'react';
import ReactModal from 'react-modal';

const CofirmModal = ({ modalOpen }) => {
  return (
    <ReactModal isOpen={ modalOpen } >

    </ReactModal>
  );
};

export default CofirmModal;
